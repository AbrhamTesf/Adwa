# OpenAI — chat completions, vision, embeddings
OPENAI_API_KEY=sk-your-openai-api-key-here

# ElevenLabs — text-to-speech for persona voices
ELEVENLABS_API_KEY=your-elevenlabs-api-key-here

# ChromaDB — remote server host:port (only with seed script --remote flag)
CHROMADB_HOST=localhost:8001

# Frontend — backend API base URL (exposed to browser)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
