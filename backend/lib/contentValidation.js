export const EXHIBIT_IDS=['shotel_sword','menelik_taytu_statue','negarit_drum','embilta','meleket'];const safe=(v,max=2000)=>typeof v==='string'&&v.length<=max&&!/[<>]/.test(v);const url=v=>safe(v,500)&&(/^(\/|https:\/\/)/.test(v))&&!/javascript:/i.test(v);
export function validateContent(input){if(!input||typeof input!=='object'||!EXHIBIT_IDS.includes(input.exhibit_id)){throw Object.assign(new Error('A valid catalog exhibit is required.'),{status:400})}if(!safe(input.name,120)||!safe(input.category,80)||!url(input.glb_url)){throw Object.assign(new Error('Name, category, and a safe model URL are required.'),{status:400})}if(!input.persona_scripts||!['material','craft','usage'].every(k=>safe(input.persona_scripts[k]))){throw Object.assign(new Error('All persona scripts are required and may not contain HTML.'),{status:400})}if(input.translations&&Object.entries(input.translations).some(([k,v])=>!['en','am','es'].includes(k)||!safe(v?.name,120))){throw Object.assign(new Error('Translations must use approved language codes.'),{status:400})}if(input.hotspot_json&&Object.values(input.hotspot_json).some(h=>!safe(h?.label,120)||!safe(h?.position,100)||!safe(h?.normal,100))){throw Object.assign(new Error('Hotspots require safe label, position, and normal values.'),{status:400})}return JSON.parse(JSON.stringify({...input,translations:input.translations||{en:{name:input.name}}}))}